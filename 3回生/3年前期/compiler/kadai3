main(){
  define N;
  define l;
  define m;
  define k;
  define a[1001];
  N = 1000;

  for(l=1; l<=N; l++){ a[l] = 1;}

  for(l=2; l<=N/2; l++){
    for(m=2; m<=N/l; m++){
    k=l*m;
    a[k] = 0;
    }
  }
}