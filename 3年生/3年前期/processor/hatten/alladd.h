
/* (add4.h) */

declare add4 {
    input a<4>, b<4>, cin;
    output sum<4>, cout;
    instrin do;

    instr_arg do(a, b, cin);
}
/* End of file (add4.h) */

/* (add8.h) */
declare add8 {
    input a<8>, b<8>, cin;
    output sum<8>, cout;
    instrin do;

    instr_arg do(a, b, cin);
}
/* End of file (add8.h) */

/* (add16.h) */
declare add16 {
    input a<16>, b<16>, cin;
    output sum<16>, cout;
    instrin do;

    instr_arg do(a, b, cin);
}
/* End of file (add32.h) */
