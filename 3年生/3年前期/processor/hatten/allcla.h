declare cla{
  input a, b, cin;
  output sum, cout;
  output pout, qout;
  instrin do;

  instr_arg do(a, b, cin);
}

declare cla2{
  input a<2>, b<2>, cin;
  output sum<2>, cout;
  output pout, qout;
  instrin do;

  instr_arg do(a, b, cin);
}

declare cla4{
  input a<4>, b<4>, cin;
  output sum<4>, cout;
  output pout, qout;
  instrin do;

  instr_arg do(a, b, cin);
}

declare cla8{
  input a<8>, b<8>, cin;
  output sum<8>, cout;
  output pout, qout;
  instrin do;

  instr_arg do(a, b, cin);
}

declare cla16{
  input a<16>, b<16>, cin;
  output sum<16>, cout;
  output pout, qout;
  instrin do;

  instr_arg do(a, b, cin);
}
